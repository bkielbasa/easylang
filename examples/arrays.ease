package main

// Comprehensive array operations demonstration
import ("strconv")

// Bubble sort
fn bubble_sort(arr: []int) {
    let n = len(arr)
    for i in 0..n {
        for j in 0..(n - i - 1) {
            if arr[j] > arr[j + 1] {
                let temp = arr[j]
                arr[j] = arr[j + 1]
                arr[j + 1] = temp
            }
        }
    }
}

// Binary search (requires sorted array)
fn binary_search(arr: []int, target: int) -> int {
    let mut left = 0
    let mut right = len(arr) - 1
    
    for left <= right {
        let mid = left + (right - left) / 2
        
        if arr[mid] == target {
            return mid
        }
        
        if arr[mid] < target {
            left = mid + 1
        } else {
            right = mid - 1
        }
    }
    
    return -1  // not found
}

// Find minimum value
fn find_min(arr: []int) -> int {
    if len(arr) == 0 {
        return 0
    }
    
    let mut min_val = arr[0]
    for i in 1..len(arr) {
        if arr[i] < min_val {
            min_val = arr[i]
        }
    }
    return min_val
}

// Find maximum value
fn find_max(arr: []int) -> int {
    if len(arr) == 0 {
        return 0
    }
    
    let mut max_val = arr[0]
    for i in 1..len(arr) {
        if arr[i] > max_val {
            max_val = arr[i]
        }
    }
    return max_val
}

// Calculate average
fn average(arr: []int) -> int {
    if len(arr) == 0 {
        return 0
    }
    
    let mut sum = 0
    for i in 0..len(arr) {
        sum = sum + arr[i]
    }
    return sum / len(arr)
}

// Print array
fn print_array(arr: []int) {
    print("[")
    for i in 0..len(arr) {
        print(strconv.Itoa(arr[i]))
        if i < len(arr) - 1 {
            print(", ")
        }
    }
    print("]")
}

fn main() -> int {
    print("=== Array Operations Demo ===\n\n")
    
    // Create an unsorted array
    let mut numbers = []int{64, 34, 25, 12, 22, 11, 90}
    
    print("Original array: ")
    print_array(numbers)
    print("\n\n")
    
    print("Min: ")
    print(strconv.Itoa(find_min(numbers)))
    print("\n")
    
    print("Max: ")
    print(strconv.Itoa(find_max(numbers)))
    print("\n")
    
    print("Average: ")
    print(strconv.Itoa(average(numbers)))
    print("\n\n")
    
    // Sort the array
    bubble_sort(numbers)
    
    print("Sorted array: ")
    print_array(numbers)
    print("\n\n")
    
    // Binary search
    let target = 22
    print("Searching for ")
    print(strconv.Itoa(target))
    print(": ")
    let index = binary_search(numbers, target)
    if index >= 0 {
        print("found at index ")
        print(strconv.Itoa(index))
    } else {
        print("not found")
    }
    print("\n\n")
    
    // Test push operation
    print("Pushing 100...\n")
    push(numbers, 100)
    print("Array after push: ")
    print_array(numbers)
    print("\n")
    
    print("New length: ")
    print(strconv.Itoa(len(numbers)))
    print("\n")
    
    return 0
}
