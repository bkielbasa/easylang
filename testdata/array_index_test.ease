fn get_element(arr: [int; 3], idx: int) -> int {
    return arr[idx]
}

fn main() -> int {
    arr := [100, 200, 300]

    // Test direct indexing
    a := arr[0]
    b := arr[1]
    c := arr[2]

    // Test indexing with variable
    i := 1
    d := arr[i]

    // Verify: a=100, b=200, c=300, d=200
    // Return: 100 + 200 + 300 + 200 = 800
    // But we'll just return d to test variable indexing
    return d
}
