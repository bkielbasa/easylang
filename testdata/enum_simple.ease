enum Color {
    Red,
    Green,
    Blue,
}

fn main() -> int {
    r := Color::Red     // tag = 0
    g := Color::Green   // tag = 1
    b := Color::Blue    // tag = 2

    // For now, just return a fixed value to verify parsing/compilation works
    // Later we'll add pattern matching to extract values
    return 42
}
