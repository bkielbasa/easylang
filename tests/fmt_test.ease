package main

struct Point { x: int, y: int }

fn (p: Point) String() -> string {
    return "(" + strconv.Itoa(p.x) + ", " + strconv.Itoa(p.y) + ")"
}

struct Color { name: string }

fn (c: Color) String() -> string {
    return c.name
}

fn TestFmtSprint(t: T) {
    p := Point { x: 3, y: 4 }
    result := fmt.Sprint(p)
    if result != "(3, 4)" {
        testing.Fatal("expected (3, 4), got " + result)
    }
}

fn TestFmtSprintDifferentTypes(t: T) {
    c := Color { name: "red" }
    result := fmt.Sprint(c)
    if result != "red" {
        testing.Fatal("expected red, got " + result)
    }
}
