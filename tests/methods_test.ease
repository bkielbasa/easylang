package main

fn TestMethodValueReceiver(t: T) {
    c := NewCounter(42)
    v := c.Value()
    if v != 42 {
        testing.Fatal("expected Value() == 42, got " + strconv.Itoa(v))
    }
}

fn TestMethodDouble(t: T) {
    c := NewCounter(21)
    d := c.Double()
    if d != 42 {
        testing.Fatal("expected Double() == 42, got " + strconv.Itoa(d))
    }
}

fn TestMethodWithArgs(t: T) {
    c := NewCounter(10)
    result := c.Add(32)
    if result != 42 {
        testing.Fatal("expected Add(32) == 42, got " + strconv.Itoa(result))
    }
}

fn TestMethodOnPoint(t: T) {
    p := make_point(3, 7)
    s := p.Sum()
    if s != 10 {
        testing.Fatal("expected Sum() == 10, got " + strconv.Itoa(s))
    }
}

fn TestMethodReturningString(t: T) {
    p := make_point(5, 12)
    desc := p.Describe()
    if desc != "(5, 12)" {
        testing.Fatal("expected (5, 12), got " + desc)
    }
}

fn TestMethodOnStructLiteral(t: T) {
    c := Counter { count: 100 }
    v := c.Value()
    if v != 100 {
        testing.Fatal("expected Value() == 100, got " + strconv.Itoa(v))
    }
}
